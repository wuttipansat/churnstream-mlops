[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "churnstream-mlops"
version = "0.1.0"
description = "Real-time churn prediction MLOps project"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]